# /dynamic-vc-rename

## 動態語音頻道重命名 (dynamic-vc-rename)

### 指令概述

`/dynamic-vc-rename` 指令讓管理員能夠設置語音頻道的動態命名功能，使頻道名稱根據伺服器數據自動更新。這可以顯示各種實時統計資訊，如在線成員數、交易數量、營收等，為伺服器增添專業度和資訊透明度。

![dynamic-vc-rename 指令介面](https://i.imgur.com/example15.png)

### 使用權限

* **需要頻道管理權限**：使用者必須擁有「管理頻道」（Manage Channels）權限才能使用此指令

### 指令參數

| 參數            | 說明                   | 必填 |
| ------------- | -------------------- | -- |
| `channel`     | 要設置動態命名的語音頻道         | ✓  |
| `rename_name` | 動態命名模板（輸入 delete 停用） | ✓  |

### 使用步驟

#### 1. 設置動態命名

選擇一個語音頻道並設定動態命名模板：

```
/dynamic-vc-rename channel:#語音頻道 rename_name:【%server_members%人】遊戲大廳
```

系統會立即更新頻道名稱，並設置為每 10 分鐘自動更新一次。

#### 2. 停用動態命名

若要停用某個頻道的動態命名功能：

```
/dynamic-vc-rename channel:#語音頻道 rename_name:delete
```

系統將停止自動更新該頻道名稱。

### 可用的佔位符

您可以在命名模板中使用以下佔位符，系統將自動替換為對應的數值：

#### 伺服器統計

| 佔位符                | 說明           |
| ------------------ | ------------ |
| `%server_members%` | 伺服器總成員數      |
| `%real_members%`   | 真實用戶數（不含機器人） |
| `%bots%`           | 機器人數量        |
| `%online_members%` | 在線成員數        |
| `%total_channels%` | 總頻道數         |
| `%voice_channels%` | 語音頻道數        |
| `%text_channels%`  | 文字頻道數        |

#### 付款統計

| 佔位符                      | 說明               |
| ------------------------ | ---------------- |
| `%all_orders%`           | 所有訂單數量           |
| `%completed_orders%`     | 已完成訂單數量          |
| `%completed_amount%`     | 已完成訂單總金額         |
| `%completed_pay_amount%` | 已完成訂單總付款金額（含手續費） |
| `%completed_fee%`        | 已收取手續費總額         |

#### 待付款統計

| 佔位符                    | 說明               |
| ---------------------- | ---------------- |
| `%pending_orders%`     | 待付款訂單數量          |
| `%pending_amount%`     | 待付款訂單總金額         |
| `%pending_pay_amount%` | 待付款訂單總付款金額（含手續費） |
| `%pending_fee%`        | 待付款訂單手續費總額       |

#### 已過期統計

| 佔位符                    | 說明               |
| ---------------------- | ---------------- |
| `%expired_orders%`     | 已過期訂單數量          |
| `%expired_amount%`     | 已過期訂單總金額         |
| `%expired_pay_amount%` | 已過期訂單總付款金額（含手續費） |
| `%expired_fee%`        | 已過期訂單手續費總額       |

#### 月度統計

| 佔位符                              | 說明                 |
| -------------------------------- | ------------------ |
| `%monthly_orders%`               | 本月所有訂單數量           |
| `%monthly_amount%`               | 本月所有訂單總金額          |
| `%monthly_pay_amount%`           | 本月所有訂單總付款金額（含手續費）  |
| `%monthly_fee%`                  | 本月手續費總額            |
| `%monthly_completed_orders%`     | 本月已完成訂單數量          |
| `%monthly_completed_amount%`     | 本月已完成訂單總金額         |
| `%monthly_completed_pay_amount%` | 本月已完成訂單總付款金額（含手續費） |
| `%monthly_completed_fee%`        | 本月已完成訂單手續費總額       |

### 命名模板範例

以下是一些實用的命名模板範例：

#### 基本統計範例

```
【%server_members%人】總聊天室
```

顯示：【125人】總聊天室

```
🌐 在線：%online_members%/%server_members%
```

顯示：🌐 在線：42/125

#### 付款統計範例

```
💰 營收：%completed_amount% 元
```

顯示：💰 營收：25600 元

```
📊 本月訂單：%monthly_completed_orders%筆
```

顯示：📊 本月訂單：78筆

#### 複合統計範例

```
💼 營收：%completed_amount%｜待付：%pending_orders%筆
```

顯示：💼 營收：25600｜待付：12筆

```
📈 月收：%monthly_completed_amount%｜訂單：%monthly_completed_orders%
```

顯示：📈 月收：8500｜訂單：32

### 系統運作機制

#### 更新頻率

系統每 10 分鐘自動更新一次所有動態命名頻道。這個頻率平衡了即時性和 Discord API 的限制。

#### Discord 頻道命名限制

使用動態命名時請注意 Discord 的以下限制：

* 頻道名稱最長 100 個字元
* 每個頻道每 10 分鐘最多可更名 2 次
* 一些特殊字元可能無法使用

#### 更新優先級

當多個頻道同時需要更新時，系統按以下優先級處理：

1. 顯示伺服器基本統計的頻道
2. 顯示付款統計的頻道
3. 顯示複合統計的頻道

### 常見問題解答

#### Q: 頻道名稱未按預期更新？

A: 可能原因：

* Discord 限制每 10 分鐘最多更改 2 次頻道名稱
* 機器人可能沒有足夠的權限修改頻道
* 命名模板可能包含無效字元

#### Q: 統計數字與預期不符？

A: 系統資料有一定的緩存時間，最長可能需要 10 分鐘才會反映最新變化。

#### Q: 多個相似佔位符的區別？

A: 請注意區分以下區別：

* `amount` 表示訂單原始金額
* `pay_amount` 表示含手續費的實際付款金額
* `fee` 單獨表示手續費金額

#### Q: 我的頻道突然不再自動更新？

A: 如果系統多次嘗試更新頻道名稱失敗（如權限不足），將自動移除該頻道的動態命名功能。請重新設置。

### 最佳實踐

#### 使用建議

1. **使用有意義的統計**：選擇與頻道用途相關的統計數據
2. **保持簡短清晰**：避免使用過多佔位符，保持名稱易讀
3. **使用表情符號**：適當的表情符號可以提升視覺效果
4. **避免過於頻繁變化**：選擇相對穩定的數據，避免頻道名稱快速變化

#### 常見用途

* **伺服器成長追蹤**：`🌱 成員：%server_members%`
* **收益透明度**：`💰 總收入：%completed_amount%`
* **活動頻道**：`🎮 遊戲室 [%online_members%人]`
* **業績指標**：`📊 本月營收：%monthly_completed_amount%`

### 相關指令

* `/payment create`：建立新的付款訂單
* `/payment review`：查詢訂單付款狀態
* `/payment history`：查看歷史付款紀錄
* `/auto-publish`：設定公告頻道自動發佈

### 支援與幫助

如使用過程中遇到任何問題，請聯繫伺服器管理員或移至指定的支援頻道尋求協助。
